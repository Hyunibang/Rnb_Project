<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper	namespace="com.dev.bean.CustomerBean">

	<insert id="signup" parameterType="customer">
		insert into customer(id, password, name, phonenumber, age, gender, animation_count, movie_count)
		values (#{id}, #{password}, #{name}, #{phonenumber}, #{age}, #{gender}, #{animation_count}, #{movie_count})
	</insert>
	
	<select id="getList" resultType="customer">
		select * from customer
	</select>
	
	<select id="signin" parameterType="String" resultType="String">
		select password
		from customer
		where id = #{id}
	</select>
	
	 <select id="findName" parameterType="HashMap" resultType="String">
		SELECT name
		FROM customer
		WHERE id=#{id} AND password=#{password}
	</select>

</mapper>