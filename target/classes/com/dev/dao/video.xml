<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper	namespace="com.dev.bean.VideoBean">
	<select id="suggestContent" resultType="video">
		SELECT v.video_id AS video_id, v.employ_id AS employ_id, v.grade_id AS grade_id, v.review_id AS review_id, v.title AS title, v.genre AS genre, v.image_number AS image_number, g.grade AS grade
		FROM video v FULL OUTER JOIN grade g
		ON (v.employ_id = g.employ_id)
		ORDER BY g.grade DESC
	</select>
</mapper>